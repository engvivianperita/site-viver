"use client";
import Link from "next/link";
import {useEffect,useState} from "react";
import {useRouter} from "next/navigation";
import {LockKeyhole,ArrowLeft,ShieldCheck} from "lucide-react";
import {getPortalSession,portalConfigured,portalRecover,portalSignIn,savePortalSession} from "@/lib/portal-auth";

export default function AreaDoCliente(){const router=useRouter();const [error,setError]=useState(""),[busy,setBusy]=useState(false),[sent,setSent]=useState(false);useEffect(()=>{if(getPortalSession())router.replace("/area-do-cliente/sistema")},[router]);async function login(form:FormData){setBusy(true);setError("");try{const session=await portalSignIn(String(form.get("email")),String(form.get("password")));savePortalSession(session);router.push("/area-do-cliente/sistema")}catch(e){setError(e instanceof Error?e.message:"Não foi possível entrar.")}finally{setBusy(false)}}async function recover(form:FormData){const email=String(form.get("email"));if(!email){setError("Informe o e-mail para recuperar a senha.");return}try{await portalRecover(email);setSent(true);setError("")}catch(e){setError(e instanceof Error?e.message:"Não foi possível enviar a recuperação.")}}return <div className="client-login-page"><section className="client-login-brand"><Link href="/" className="client-back"><ArrowLeft size={16}/> Voltar ao site</Link><img src="/brand/logo-viver-negativa-legivel-v3.png" alt="Viver Segurança no Trabalho"/><div><span>ÁREA EXCLUSIVA</span><h1>Gestão SSMA da sua empresa em um só lugar.</h1><p>Acompanhe documentos, RDOs, auditorias, indicadores, riscos e planos de ação com acesso protegido.</p><div className="client-security"><ShieldCheck/><span>Dados separados por empresa<br/><small>Ambiente seguro e rastreável</small></span></div></div></section><section className="client-login-form"><div className="client-login-card"><div className="client-lock"><LockKeyhole/></div><span>PORTAL DO CLIENTE</span><h2>Acesse o Integra SSMA</h2><p>Informe o usuário e a senha fornecidos pela Viver.</p>{!portalConfigured&&<div className="client-alert">A base online está preparada, mas ainda precisa receber as credenciais do projeto Supabase.</div>}<form action={login}><label>E-mail do usuário<input name="email" type="email" placeholder="usuario@empresa.com.br" autoComplete="email" required/></label><label>Senha<input name="password" type="password" autoComplete="current-password" required/></label>{error&&<div className="client-error">{error}</div>}{sent&&<div className="client-success">Enviamos as instruções de recuperação para o e-mail informado.</div>}<button className="button" disabled={busy||!portalConfigured}>{busy?"Entrando...":"Entrar na Área do Cliente"}</button><button type="submit" formAction={recover} className="client-forgot">Esqueci minha senha</button></form><small>Problemas com o acesso? Fale com a Viver pelo canal de atendimento.</small></div></section></div>}
