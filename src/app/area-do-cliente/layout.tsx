import type {Metadata} from "next";
export const metadata:Metadata={title:"Área do Cliente · Integra SSMA",description:"Acesso protegido ao sistema de gestão SSMA da Viver.",robots:{index:false,follow:false,nocache:true}};
export default function ClientAreaLayout({children}:{children:React.ReactNode}){return children}
