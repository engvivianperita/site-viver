import Link from "next/link"; import type { ReactNode } from "react"; import { ChevronRight } from "lucide-react";
export function SectionHeading({eyebrow,title,text,center=false}:{eyebrow?:string;title:string;text?:string;center?:boolean}){return <div className={`section-heading ${center?"center":""}`}>{eyebrow&&<span className="eyebrow">{eyebrow}</span>}<h2>{title}</h2>{text&&<p>{text}</p>}</div>}
export function PageHero({eyebrow,title,text}:{eyebrow:string;title:string;text:string}){return <section className="page-hero"><div className="container"><span className="eyebrow light">{eyebrow}</span><h1>{title}</h1><p>{text}</p></div></section>}
export function Breadcrumbs({current}:{current:string}){return <nav className="breadcrumbs container" aria-label="Navegação estrutural"><Link href="/">Início</Link><ChevronRight size={14}/><span aria-current="page">{current}</span></nav>}
export function CTASection(){return <section className="cta"><div className="container cta-inner"><div><span className="eyebrow light">Vamos conversar</span><h2>Prepare sua empresa para contratos mais seguros, organizados e sustentáveis.</h2></div><div className="actions"><Link className="button lime" href="/contato">Solicitar diagnóstico</Link><Link className="button ghost" href="/contato">Falar pelo WhatsApp</Link></div></div></section>}
export function Cards({items}:{items:{title:string;text:string}[]}){return <div className="cards">{items.map(x=><article className="card" key={x.title}><h3>{x.title}</h3><p>{x.text}</p></article>)}</div>}
export function ContentShell({children}:{children:ReactNode}){return <div className="prose container">{children}</div>}
