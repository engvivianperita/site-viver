"use client"; import { ChevronDown, Check } from "lucide-react"; import { serviceGroups } from "@/data/services";
export function ServiceAccordion(){return <div className="accordions">{serviceGroups.map((s,i)=><details key={s.title} open={i===0}><summary><span><small>{String(i+1).padStart(2,"0")}</small>{s.title}</span><ChevronDown aria-hidden="true"/></summary><div className="accordion-body">{s.intro&&<p className="intro">{s.intro}</p>}<ul>{s.items.map(x=><li key={x}><Check size={17}/><span>{x}</span></li>)}</ul>{s.subsections?.map(sub=><div className="subsection" key={sub.title}><h3>{sub.title}</h3><ul>{sub.items.map(x=><li key={x}><Check size={17}/><span>{x}</span></li>)}</ul></div>)}{s.note&&<p className="note">{s.note}</p>}</div></details>)}</div>}
